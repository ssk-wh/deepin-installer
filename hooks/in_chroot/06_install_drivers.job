#!/bin/bash

utcs_check() {
    if which /usr/sbin/utcs; then
        return 0
    else
        return 1
    fi
}

utcs_start() {
    /usr/sbin/utcs
}

main() {
    if utcs_check; then
        utcs_start
    else
        warn "the /usr/sbin/utcs tool module could not be found"
    fi
}

main
